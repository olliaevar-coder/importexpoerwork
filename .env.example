# Discord application bot token (never commit a real token)
DISCORD_TOKEN=
CLIENT_ID=
# Commands are registered only to this guild, and update quickly.
GUILD_ID=
# Channel where staff receive submitted orders.
STAFF_CHANNEL_ID=
# Optional: comma-separated role IDs allowed to use staff controls. Empty = anyone with ManageGuild.
STAFF_ROLE_IDS=
